<br>
<br>

<div class="row transGrey dropShadow">

  <div class="col-md-12 margin20">
  <div class="col-md-6">
    <div class="">
      <%= image_tag @listing.image.url, class: "dropShadow" %>
    </div>
  </div>

  <div class="col-md-6 dropShadow infoPad">
    <h3><%= @listing.name %></h3>
    <p><%= number_to_currency(@listing.price) %></p>
    <p><%= @listing.description %></p>
    <br>
    <div class="center">
      <%if !(current_user == @listing.user)%>
      <%= link_to "Buy It Now", new_listing_order_path(@listing), class: "btn btn-primary white"%>
      <% end %>
    </div>
  </div>
  </div>

</div>
<%if (current_user == @listing.user)%>

<% end %>
<br>
<%= link_to 'Back', listings_path, class:"btn btn-primary white" %> | <%= link_to 'Edit', edit_listing_path(@listing), class:"btn btn-warning" %>
